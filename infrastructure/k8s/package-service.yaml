apiVersion: apps/v1
kind: Deployment
metadata:
  name: package-service
  namespace: travelnest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: package-service
  template:
    metadata:
      labels:
        app: package-service
    spec:
      containers:
      - name: package-service
        image: ap-mumbai-1.ocir.io/namespace/package-service:latest # Replace namespace placeholder inside Jenkins
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: travelnest-config
        - secretRef:
            name: db-secret
        - secretRef:
            name: rabbitmq-secret
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: package-service
  namespace: travelnest
spec:
  type: ClusterIP
  selector:
    app: package-service
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: package-service-hpa
  namespace: travelnest
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: package-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
