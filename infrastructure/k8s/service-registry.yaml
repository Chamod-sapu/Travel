apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-registry
  namespace: travelnest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-registry
  template:
    metadata:
      labels:
        app: service-registry
    spec:
      containers:
      - name: service-registry
        image: ap-mumbai-1.ocir.io/namespace/service-registry:latest # Replace namespace placeholder inside Jenkins
        imagePullPolicy: Always
        ports:
        - containerPort: 8761
        envFrom:
        - configMapRef:
            name: travelnest-config
        - secretRef:
            name: db-secret
        - secretRef:
            name: jwt-secret
        - secretRef:
            name: rabbitmq-secret
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8761
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8761
          initialDelaySeconds: 60
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: service-registry
  namespace: travelnest
spec:
  type: ClusterIP
  selector:
    app: service-registry
  ports:
  - protocol: TCP
    port: 8761
    targetPort: 8761
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: service-registry-hpa
  namespace: travelnest
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: service-registry
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
