apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: travelnest
type: Opaque
data:
  # Placeholder for base64 encoded DB credentials
  # run: echo -n "travelnest" | base64 -> dHJhdmVsbmVzdA==
  # run: echo -n "Qwertyuiop@1234" | base64 -> UXdlcnR5dWlvcEAxMjM0
  db-username: dHJhdmVsbmVzdA==  # Replace with actual base64 DB username if changed
  db-password: UXdlcnR5dWlvcEAxMjM0  # Replace with actual base64 DB password if changed
  jwt-private-key: # Replace me with base64 encoded private key
  jwt-public-key: # Replace me with base64 encoded public key
